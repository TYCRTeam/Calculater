namespace Calculate.Calculator
{
    // 元素类型
    internal enum ElementType
    {
        OPERAND,// 操作数
        OPERATOR// 操作符
    }

    // 操作符类型
    internal enum OperatorType
    {
        ADD,
        CUT,
        DIVIDE,
        MUTIPLY,
        L_BRACKET,
        R_BRACKET
    }
}
